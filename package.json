{
  "name": "constelacao-agendamento",
  "version": "1.0.0",
  "private": true,
  "description": "Página pública de agendamento de Constelação com Katia Melo.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "lint": "node --check server.js && node --check public/app.js && node --check public/admin.js",
    "typecheck": "node --check server.js && node --check public/app.js && node --check public/admin.js"
  },
  "engines": {
    "node": ">=18"
  }
}
